# ContextWeave v0.1 开发进度

**修订号：** r8
**状态：** 发布候选验证完成，待用户最终验收
**执行文档：** [v0.1-execution-r6.md](../versions/v0.1-execution-r6.md)，已确认
**最后更新：** 2026-09-22

## 当前阶段

v0.1 执行文档 r6 的实现范围已完成，正式代码位于 `codex/v0.1-base-ui` 分支，最新代码提交为 `83c7c48`。`v0.1.0-alpha.5` 是加入 SPDX SBOM、升级官方 Actions 并重新验证后的最终预览 tag；它没有覆盖此前的 `v0.1.0-alpha.1` 至 `v0.1.0-alpha.4` tag。

GitHub Actions run [35635364171](https://github.com/mik-myp/contextweave/actions/runs/35635364171) 已完成，Windows x64、macOS arm64、macOS x64 的检查、构建、产物上传、SPDX SBOM 生成和 GitHub Release 发布全部成功。Release 地址为 [v0.1.0-alpha.5](https://github.com/mik-myp/contextweave/releases/tag/v0.1.0-alpha.5)。根据仓库流程，仍需用户完成最终验收后才能把 v0.1 标记为最终完成。

## 已完成

- 创建并维护 `AGENTS.md`、版本分析、执行文档、ADR、兼容矩阵、风险登记和进度证据。
- 初始化 pnpm workspace、Electron 44、React 19、TypeScript、Vite 7、electron-vite 和 electron-builder 基线。
- 使用官方 shadcn/ui `base-nova` 与 Base UI primitives，移除 Radix UI 直接依赖；按官方 `sidebar-07` 结构使用 `SidebarProvider`、`SidebarInset`、可收缩侧栏和官方空白主体组合。
- 完成个人环境、代理、内核、设置、Worker smoke 的本地 UI 和类型化 Preload/IPC 闭环。
- 主题系统支持 light/dark/system、颜色预设、圆角、密度、字体和 Sidebar 布局，并通过版本化 contracts、受限 IPC 和 SQLite 设置持久化。
- 完成 SQLite migration、环境和代理元数据、内核 Registry/Adapter、独占运行锁、恢复状态和 Worker CDP 控制链路。
- 使用 Electron `safeStorage` 保存代理凭据；代理密码不进入浏览器命令行、URL、环境变量或日志，按 ADR 0002 通过 Worker 标准输入和 CDP 认证挑战传递。
- 工程正式门禁为 Prettier、ESLint、TypeScript 和 Vitest；Oxlint/Oxfmt 保留为渐进试运行，不替换 Electron 根链路。
- 补齐 `.github/workflows/ci.yml`、`build-desktop.yml`、`release.yml`，覆盖检查、Windows x64、macOS arm64/x64 构建、SHA-256 和 tag Release 发布。
- 修正 Windows workflow 使用 `Get-FileHash.Path` 生成资产文件名；alpha.4 及后续校验文件包含哈希和对应文件名。
- 发布 workflow 使用 `anchore/sbom-action@v0.24.2` 生成 SPDX 2.3 SBOM，并将其作为 Release 资产上传；checkout、setup-node、upload/download-artifact 使用可用的 v5。

## 自动化验证

本地在 macOS arm64、Node.js 22.15.0、pnpm 10.26.2 下通过：

```text
pnpm install --frozen-lockfile
pnpm check
pnpm --filter @contextweave/desktop exec electron-vite build
git diff --check
```

`pnpm check` 包含 Prettier、ESLint、TypeScript 和 Vitest；所有 workspace 测试通过。SQLite 测试只产生 Node `node:sqlite` 实验性警告，不影响退出码。三个 workflow 文件通过 Ruby YAML 解析。

GitHub Actions `35635364171` 的结果：

| Job | Runner | 结果 |
|---|---|---|
| Build windows-x64 | `windows-latest` | success，4m40s |
| Build macos-arm64 | `macos-14` | success，2m23s |
| Build macos-x64 | `macos-15-intel` | success，4m51s |
| Publish GitHub Release | `ubuntu-latest` | success，28s；SPDX 2.3 SBOM 生成成功 |

GitHub Actions 仍对 `actions/upload-artifact@v5` 和 `pnpm/action-setup@v4` 显示 Node 20 runtime 弃用提示；任务成功，后续可在这些 action 提供 Node 24 版本后继续升级。

## Release 产物和校验

Release 使用 tag 对应提交 `83c7c48979ab5666ca205fac914bdfcda28bf338` 构建。已下载三个 `SHA256SUMS-*.txt` 文件，并将其中 11 个产物的哈希逐项与 GitHub Release API 的 asset digest 核对，全部匹配；`contextweave.spdx.json` 的 SPDX 2.3 结构和 898 个 package 条目也已验证。

| 平台 | 产物 | SHA-256 |
|---|---|---|
| macOS arm64 | `ContextWeave-0.1.0-alpha.5-mac-arm64.dmg` | `89633c66479dc151e92e8ee4aefa8e6459d325574af700f3555e0271bfbba983` |
| macOS arm64 | `ContextWeave-0.1.0-alpha.5-mac-arm64.zip` | `eee339bf146a1dc0102672b2fbbb0951f9c65d74fd35e2ced362bab4f01bd08c` |
| macOS x64 | `ContextWeave-0.1.0-alpha.5-mac-x64.dmg` | `3012481aab27265fdfefc4fdfd5f97d05dac7012ad9ae635cd1f5208c6fa8c34` |
| macOS x64 | `ContextWeave-0.1.0-alpha.5-mac-x64.zip` | `d149d6f1b409047592f12a857b7013a8116c1ec1c353d4de458fbfbeda4481a9` |
| Windows x64 | `ContextWeave-0.1.0-alpha.5-win-x64-portable.exe` | `31056f38d28f896400f3ed42896d525257a696028cb999186778655119067433` |
| Windows x64 | `ContextWeave-0.1.0-alpha.5-win-x64-setup.exe` | `4f1c57bbd2d86db1b460b47129401614562797a4b4df70c3d24411d9a696bc40` |
| Release metadata | `contextweave.spdx.json` | `253e97ce195019bd9f7238e30a89d1226675e3cae80a9a9c9b24504b83dd94f2` |

每个平台的 `.blockmap` 文件也已包含在 Release 和对应校验文件中；校验文件本身由 workflow 生成并上传。产物目前未签名或公证。

## 仍未关闭的验收项和已知限制

- `fingerprint-chromium` 当前只有适配器、配置 schema 和未配置 manifest；没有已确认来源、许可证、下载 URL 和 SHA-256，因此不能宣称可下载或分发该内核。标准 Chromium 只探测用户本机已有且用户有权使用的 Chromium/Chrome/Edge。
- 已完成标准 Chromium 的打包 Worker CDP smoke；使用专用测试代理完成 HTTP、HTTPS、SOCKS5 认证矩阵仍待后续验收。
- 桌面 UI 自动化、异常退出的跨平台手动矩阵、低磁盘和下载中断测试尚未全部自动化覆盖。
- 本机 macOS arm64 的 `electron-builder --dir` 曾在 packaging 阶段长时间无输出而中止；三个目标平台的 CI Release 构建均已通过，因此 CI 是本轮 macOS 产物证据来源。
- Release 产物未签名或公证，首次启动可能需要按系统提示手动放行。
- v0.1 不包含团队 API、PostgreSQL、WebUI、工作流、LangChain/LangGraph、RAG、远程执行、备份同步或完整指纹隐身承诺。

## 历次 Release 验证记录

- `v0.1.0-alpha.1`：Windows 因 CRLF 导致 Prettier 检查失败，已保留 tag 和失败记录。
- `v0.1.0-alpha.2`：Windows/macOS arm64 通过；macOS x64 使用 `macos-13` 长时间排队后取消，已改用可用的 `macos-15-intel`。
- `v0.1.0-alpha.3`：三个平台和 Release 成功，但 Windows 校验文件缺少文件名，未作为最终验证 tag。
- `v0.1.0-alpha.4`：修正校验文件并同步应用版本号后重新构建，三个平台、Release 和 11 个资产哈希核对全部成功。
- `v0.1.0-alpha.5`：加入 SPDX 2.3 SBOM 和官方 Actions 升级；三个平台、Release、11 个资产哈希及 898 个 SBOM package 条目全部验证成功。

## 可复核证据

- 版本分析：[../versions/v0.1-analysis.md](../versions/v0.1-analysis.md)
- 执行文档：[../versions/v0.1-execution-r6.md](../versions/v0.1-execution-r6.md)
- 开发约束：[../../AGENTS.md](../../AGENTS.md)
- 文档索引：[../README.md](../README.md)
- SQLite ADR：[../adr/0001-local-sqlite-runtime.md](../adr/0001-local-sqlite-runtime.md)
- 代理认证 ADR：[../adr/0002-proxy-auth-transport.md](../adr/0002-proxy-auth-transport.md)
- 兼容矩阵：[../compatibility-matrix.md](../compatibility-matrix.md)
- 风险登记：[../risk-register.md](../risk-register.md)
- GitHub Actions run：[35635364171](https://github.com/mik-myp/contextweave/actions/runs/35635364171)
- GitHub Release：[v0.1.0-alpha.5](https://github.com/mik-myp/contextweave/releases/tag/v0.1.0-alpha.5)
