# ContextWeave v0.1 开发进度

**修订号：** r16
**状态：** 开发中（个人版交互已实现，主体视觉收敛待用户验收）
**执行文档：** [v0.1-execution-r6.md](../versions/v0.1-execution-r6.md)，已确认
**最后更新：** 2026-09-22

## 当前阶段

v0.1 已发布预览构建，仍有功能和验收项未关闭；正式代码位于 `codex/v0.1-base-ui` 分支，本轮个人版交互改动正在当前工作区复核。`v0.1.0-alpha.5` 是加入 SPDX SBOM、升级官方 Actions 并重新验证后的最终预览 tag；它没有覆盖此前的 `v0.1.0-alpha.1` 至 `v0.1.0-alpha.4` tag。

GitHub Actions run [35635364171](https://github.com/mik-myp/contextweave/actions/runs/35635364171) 已完成，Windows x64、macOS arm64、macOS x64 的检查、构建、产物上传、SPDX SBOM 生成和 GitHub Release 发布全部成功。Release 地址为 [v0.1.0-alpha.5](https://github.com/mik-myp/contextweave/releases/tag/v0.1.0-alpha.5)。用户于 2026-09-22 认可当前整体内容并要求继续完善个人版；这不代表下文未关闭项已经通过。

## 已完成

- 创建并维护 `AGENTS.md`、版本分析、执行文档、ADR、兼容矩阵、风险登记和进度证据。
- 初始化 pnpm workspace、Electron 44、React 19、TypeScript、Vite 7、electron-vite 和 electron-builder 基线。
- 使用官方 shadcn/ui `base-nova` 与 Base UI primitives，移除 Radix UI 直接依赖；按官方 `sidebar-07` 结构使用 `SidebarProvider`、`SidebarInset`、可收缩侧栏和官方空白主体组合。
- 完成个人环境、代理、内核、设置、Worker smoke 的本地 UI 和类型化 Preload/IPC 闭环。
- 主题系统支持 light/dark/system、颜色预设、圆角、密度、字体和 Sidebar 布局，并通过版本化 contracts、受限 IPC 和 SQLite 设置持久化。
- 完成 SQLite migration、环境和代理元数据、内核 Registry/Adapter、独占运行锁、恢复状态和 Worker CDP 控制链路。
- 使用 Electron `safeStorage` 保存代理凭据；代理密码不进入浏览器命令行、URL、环境变量或日志，按 ADR 0002 通过 Worker 标准输入和 CDP 认证挑战传递。
- 工程正式门禁为 Prettier、ESLint、TypeScript 和 Vitest；Oxlint/Oxfmt 保留为渐进试运行，不替换 Electron 根链路。
- 补齐 `.github/workflows/ci.yml`、`build-desktop.yml`、`release.yml`，覆盖检查、Windows x64、macOS arm64/x64 构建、SHA-256 和 tag Release 发布。
- 修正 Windows workflow 使用 `Get-FileHash.Path` 生成资产文件名；alpha.4 及后续校验文件包含哈希和对应文件名。
- 发布 workflow 使用 `anchore/sbom-action@v0.24.2` 生成 SPDX 2.3 SBOM，并将其作为 Release 资产上传；checkout、setup-node、upload/download-artifact 使用可用的 v5。

## 自动化验证

本地在 macOS arm64、Node.js 22.15.0、pnpm 10.26.2 下通过：

```text
pnpm install --frozen-lockfile
pnpm check
pnpm --filter @contextweave/desktop exec electron-vite build
git diff --check
```

`pnpm check` 包含 Prettier、ESLint、TypeScript 和 Vitest；所有 workspace 测试通过。SQLite 测试只产生 Node `node:sqlite` 实验性警告，不影响退出码。三个 workflow 文件通过 Ruby YAML 解析。

GitHub Actions `35635364171` 的结果：

| Job | Runner | 结果 |
|---|---|---|
| Build windows-x64 | `windows-latest` | success，4m40s |
| Build macos-arm64 | `macos-14` | success，2m23s |
| Build macos-x64 | `macos-15-intel` | success，4m51s |
| Publish GitHub Release | `ubuntu-latest` | success，28s；SPDX 2.3 SBOM 生成成功 |

GitHub Actions 仍对 `actions/upload-artifact@v5` 和 `pnpm/action-setup@v4` 显示 Node 20 runtime 弃用提示；任务成功，后续可在这些 action 提供 Node 24 版本后继续升级。

## Release 产物和校验

Release 使用 tag 对应提交 `83c7c48979ab5666ca205fac914bdfcda28bf338` 构建。已下载三个 `SHA256SUMS-*.txt` 文件，并将其中 11 个产物的哈希逐项与 GitHub Release API 的 asset digest 核对，全部匹配；`contextweave.spdx.json` 的 SPDX 2.3 结构和 898 个 package 条目也已验证。

| 平台 | 产物 | SHA-256 |
|---|---|---|
| macOS arm64 | `ContextWeave-0.1.0-alpha.5-mac-arm64.dmg` | `89633c66479dc151e92e8ee4aefa8e6459d325574af700f3555e0271bfbba983` |
| macOS arm64 | `ContextWeave-0.1.0-alpha.5-mac-arm64.zip` | `eee339bf146a1dc0102672b2fbbb0951f9c65d74fd35e2ced362bab4f01bd08c` |
| macOS x64 | `ContextWeave-0.1.0-alpha.5-mac-x64.dmg` | `3012481aab27265fdfefc4fdfd5f97d05dac7012ad9ae635cd1f5208c6fa8c34` |
| macOS x64 | `ContextWeave-0.1.0-alpha.5-mac-x64.zip` | `d149d6f1b409047592f12a857b7013a8116c1ec1c353d4de458fbfbeda4481a9` |
| Windows x64 | `ContextWeave-0.1.0-alpha.5-win-x64-portable.exe` | `31056f38d28f896400f3ed42896d525257a696028cb999186778655119067433` |
| Windows x64 | `ContextWeave-0.1.0-alpha.5-win-x64-setup.exe` | `4f1c57bbd2d86db1b460b47129401614562797a4b4df70c3d24411d9a696bc40` |
| Release metadata | `contextweave.spdx.json` | `253e97ce195019bd9f7238e30a89d1226675e3cae80a9a9c9b24504b83dd94f2` |

每个平台的 `.blockmap` 文件也已包含在 Release 和对应校验文件中；校验文件本身由 workflow 生成并上传。产物目前未签名或公证。

## 仍未关闭的验收项和已知限制

- `fingerprint-chromium` 当前只有适配器、配置 schema 和未配置 manifest；没有已确认来源、许可证、下载 URL 和 SHA-256，因此不能宣称可下载或分发该内核。标准 Chromium 只探测用户本机已有且用户有权使用的 Chromium/Chrome/Edge。
- 已完成标准 Chromium 的打包 Worker CDP smoke；使用专用测试代理完成 HTTP、HTTPS、SOCKS5 认证矩阵仍待后续验收。
- 桌面 UI 自动化、异常退出的跨平台手动矩阵、低磁盘和下载中断测试尚未全部自动化覆盖。
- 本机 macOS arm64 的 `electron-builder --dir` 曾在 packaging 阶段长时间无输出而中止；三个目标平台的 CI Release 构建均已通过，因此 CI 是本轮 macOS 产物证据来源。
- Release 产物未签名或公证，首次启动可能需要按系统提示手动放行。
- v0.1 不包含团队 API、PostgreSQL、WebUI、工作流、LangChain/LangGraph、RAG、远程执行、备份同步或完整指纹隐身承诺。

## 历次 Release 验证记录

- `v0.1.0-alpha.1`：Windows 因 CRLF 导致 Prettier 检查失败，已保留 tag 和失败记录。
- `v0.1.0-alpha.2`：Windows/macOS arm64 通过；macOS x64 使用 `macos-13` 长时间排队后取消，已改用可用的 `macos-15-intel`。
- `v0.1.0-alpha.3`：三个平台和 Release 成功，但 Windows 校验文件缺少文件名，未作为最终验证 tag。
- `v0.1.0-alpha.4`：修正校验文件并同步应用版本号后重新构建，三个平台、Release 和 11 个资产哈希核对全部成功。
- `v0.1.0-alpha.5`：加入 SPDX 2.3 SBOM 和官方 Actions 升级；三个平台、Release、11 个资产哈希及 898 个 SBOM package 条目全部验证成功。

## 可复核证据

- 版本分析：[../versions/v0.1-analysis.md](../versions/v0.1-analysis.md)
- 执行文档：[../versions/v0.1-execution-r6.md](../versions/v0.1-execution-r6.md)
- 开发约束：[../../AGENTS.md](../../AGENTS.md)
- 文档索引：[../README.md](../README.md)
- SQLite ADR：[../adr/0001-local-sqlite-runtime.md](../adr/0001-local-sqlite-runtime.md)
- 代理认证 ADR：[../adr/0002-proxy-auth-transport.md](../adr/0002-proxy-auth-transport.md)
- 兼容矩阵：[../compatibility-matrix.md](../compatibility-matrix.md)
- 风险登记：[../risk-register.md](../risk-register.md)
- GitHub Actions run：[35635364171](https://github.com/mik-myp/contextweave/actions/runs/35635364171)
- GitHub Release：[v0.1.0-alpha.5](https://github.com/mik-myp/contextweave/releases/tag/v0.1.0-alpha.5)

## 2026-09-22 个人版交互完善（执行中）

- 用户确认当前整体内容可以过关，继续 v0.1；本轮不改变团队、内核分发和发布边界。
- CW-UI-01：主导航收敛为环境、代理、内核、运行记录；注释团队切换挂载，顶部显示个人工作区；底部本地用户菜单提供设置和关于，移除账户、账单、订阅、通知和退出登录占位项。
- CW-UI-02：环境、代理采用 Table 列表 + Dialog 新建/编辑 + AlertDialog 删除确认；空态、提交防重、失败保留表单、禁用状态与错误反馈均使用已安装 shadcn Base UI 组件；所有下拉选择使用 Select。
- CW-ENV-01：补齐本地环境名称和代理绑定编辑、环境记录删除。更新输入带 version=1，通过受限 IPC 校验；数据库结构不变，保持内核、profile 路径和其余配置不变。运行中或需恢复的环境禁止编辑/删除；删除仅移除环境记录，保留用户目录与运行历史，确认框明确说明。
- CW-PROXY-01：被环境引用的代理禁止删除，运行中环境引用的代理禁止编辑；环境下次启动读取最新已保存代理，避免继续使用创建时的旧配置。
- 验证结果：`pnpm check`、`pnpm --filter @contextweave/desktop exec electron-vite build` 和 `git diff --check` 均通过；contracts、storage、environment-management 测试共 9 个通过。开发窗口手动确认个人侧栏、用户菜单、设置/关于入口、环境/代理 Dialog 和 Base UI Select 可用。构建仍有 Zod 第三方 PURE 注释警告，不影响产物。
- 本轮不新增运行时依赖，不覆盖已发布 tag；原有内核来源、代理认证矩阵及异常测试缺口继续保留。

## 2026-09-22 GitHub UI 源码研究

- 完成对 shadcn-admin、new-api、Simprint、CloakBrowser Manager 和 vue-vben-admin 固定 commit 的静态源码审阅，重点覆盖 B 端 CRUD、指纹环境列表、批量操作、复杂编辑 Drawer、系统设置和主题偏好。
- 研究结论已整理到 [B 端 CRUD、指纹环境管理与设置页研究](../research/ui-crud-settings-patterns.md)。当前结论支持继续使用 v0.1 已实现的环境/代理 `Table + Dialog/AlertDialog`，将分组、标签、回收站、批量环境操作和完整指纹配置工作区保留为后续版本候选。
- 本次只新增研究文档和进度证据，不新增代码、依赖、数据库字段、IPC 或 v0.1 执行范围；AGENTS 和文档索引已登记研究文档。

## 2026-09-22 页面级标题收敛

- 用户明确不保留环境、代理、内核、设置、关于、运行记录和指纹策略页面顶部的大标题与说明；这些信息由 Sidebar 和顶部面包屑表达。
- 已从 `apps/desktop/src/App.tsx` 移除业务页面外层 `h1 + p` 区块，保留卡片内部用于数量、状态、安全边界和配置项的说明。
- CW-UI-03：环境列表增加文本搜索和状态 `Select` 筛选，代理列表增加文本搜索；筛选无结果与列表为空分别使用 `Empty` 表达，并提供重置操作。
- CW-UI-04：环境和代理的编辑/删除动作统一收进 Base UI `DropdownMenu`，保留危险删除的 destructive 语义和运行状态限制。
- 验证结果：`pnpm check`、`pnpm --filter @contextweave/desktop exec electron-vite build`、`git diff --check` 均通过；`pnpm dev` 成功启动 Electron/Vite（5173 被占用时自动使用 5174），随后手动停止开发进程。
- 本轮不改变数据、IPC、权限或页面功能范围。

## 2026-09-22 Base UI 表单用法复核

- 按当前 shadcn/ui `base-nova`/Base UI 组件规范，将环境和代理新建/编辑表单、主题设置选择器统一为 `FieldGroup`、`Field`、`FieldLabel` 与 `Select` 组合，并为输入控件补齐稳定的关联 ID。
- 环境和代理行操作菜单的菜单项统一置于 `DropdownMenuGroup`；页面剩余 `space-y-*` 布局改为 `flex` + `gap-*`，不改变视觉意图和业务行为。
- 为带文字的按钮图标补充官方 `data-icon="inline-start"` 标记，保持 Base UI Button 的间距和可访问组件组合方式。
- 验证结果：`pnpm check`、`pnpm --filter @contextweave/desktop exec electron-vite build` 和 `git diff --check` 均通过；构建仍仅有 Zod 第三方 PURE 注释警告。
- 本轮不新增运行时依赖，不改变数据、IPC、权限、页面范围或发布 tag。

## 2026-09-22 主体内容统一重构

- 根据 GitHub 上 shadcn-admin、new-api、Simprint、CloakBrowser Manager 和 vue-vben-admin 的源码研究，重新设计业务页面主体，不再使用每个页面独立的顶部标题/描述和多层 Card 边框。
- 环境与代理统一为“数量/当前选择 + 搜索/筛选/创建工具栏 → 单一数据表面 → 选中环境的底部动作条”；空态仍使用 `Empty`，行操作仍使用 `DropdownMenu`。
- 内核从多张 Card 改为统一的资源行，状态、安装动作、能力和路径在同一行内按信息优先级排列。
- 设置改为外观与本地运行时的左右分区；运行记录、关于和指纹策略改为轻量内容面板。仅保留必要的局部标签、状态和安全边界说明，不恢复页面级大标题。
- 主体表面使用单一内容流和必要的行分隔线；Sidebar、顶部面包屑、Dialog、Select、Badge、Table 等官方组件和交互契约保持不变。
- 开发窗口手动复核环境、代理、内核和运行记录页，页面结构统一且无运行时错误；`pnpm check`、`electron-vite build` 和 `git diff --check` 均通过。
- 本轮不新增数据、IPC、权限或运行时依赖，不引入分组、标签、回收站、批量环境操作和完整指纹配置工作区。

## 2026-09-22 主体视觉进一步收敛（执行中）

- 根据用户对“边框过多、结构不统一”的反馈，移除环境和代理列表外层卡片背景、圆角和阴影，改为工具栏下的单一表格内容流；仅保留工具栏分隔线、表头/行分隔线和选中环境动作条的层级提示。
- 内核改为同一 `divide-y` 资源列表，设置、运行记录、关于和指纹策略改为无卡片内容区；局部说明仅用于解释设置项、运行时边界和未配置内核，不再承担页面标题或页面描述职责。
- 各主体页继续共享“状态/数量 → 工具栏 → 内容列表/资源行 → 局部动作”的结构，保持 Base UI `Table`、`Select`、`DropdownMenu`、`Dialog`、`AlertDialog`、`Empty` 和 `Badge` 的交互不变。
- 本轮只调整 Renderer 布局 class，不新增数据、IPC、权限、依赖或 v0.1 功能范围。
- 验证结果：`pnpm check`、`pnpm --filter @contextweave/desktop exec electron-vite build` 和 `git diff --check` 均通过；开发窗口成功启动，环境页无页面级标题，主体呈现为工具栏、单一空态内容流和顶部面包屑。构建仍仅有 Zod 第三方 PURE 注释警告，SQLite 测试仍有 Node 实验性警告，均不影响退出码。

## 2026-09-22 GitHub CRUD 与设置页源码复核

- 使用 agent-browser 重新读取 shadcn-admin、new-api、Simprint、CloakBrowser Manager 和 vue-vben-admin 固定 commit 的代表性源码，核对列表状态、工具栏、批量动作、行菜单、复杂编辑容器和设置 section 的真实实现路径。
- 研究结论已追加到 [B 端 CRUD、指纹环境管理与设置页研究](../research/ui-crud-settings-patterns.md) r5：表格状态需要独立建模，简单对象使用 Dialog，复杂指纹环境使用分区 Sheet/Drawer，设置按 section 和 Provider 组织，环境生命周期与业务/同步状态分离。
- 本轮只更新研究文档和进度证据，不扩大 v0.1 功能范围，也不复制被审阅项目的代码、样式或资源。

## 2026-09-22 React Router Data Mode 与 Renderer 目录重构

- 新增 `react-router@8.4.0`，使用 `createHashRouter` + `RouterProvider` 组织 Data Mode 路由；菜单 URL 和页面标题集中在 `src/shared/config/navigation.ts`，不再通过手工 `page` 状态切换页面。
- 删除单体 `src/App.tsx`，按 `app/`、`features/<feature>/pages`、`features/<feature>/components`、`shared/` 拆分应用壳层、环境、代理、内核、设置、运行记录、指纹策略和关于页面。
- 环境和代理页面的工具栏、列表、动作条、表单 Dialog、删除确认分别独立成组件文件；每个业务 `.tsx` 文件只导出一个 React 组件。
- 验证结果：`pnpm check`、`pnpm --filter @contextweave/desktop exec electron-vite build`、`git diff --check` 和开发窗口启动 smoke 均通过；Node 26 相对仓库要求的 Node 22.15.x 仍会产生引擎警告，构建仍有 Zod 第三方 PURE 注释警告。

## 2026-09-22 主体数据表面按 B 端模式调整

- 主工作区增加统一的最大内容宽度和轻微背景层，页面主体保持同一内容画布。
- 环境和代理列表改为单一圆角数据表面，表头、hover、选中态和空态使用同一层级；选中环境动作条使用轻量 primary tint，保留启动、停止、恢复和 Worker Smoke。
- 本轮只调整 Renderer 布局 class，不新增数据、IPC、权限或页面功能；开发窗口复核环境和代理空态布局，未发现运行时错误。
- 验证结果：`pnpm --filter @contextweave/desktop format:check`、`pnpm --filter @contextweave/desktop typecheck`、`pnpm --filter @contextweave/desktop lint`、`electron-vite build` 和 `git diff --check` 均通过。

## 2026-09-22 主体组件布局结构收敛

- 新增跨页面 `WorkspacePage` 布局组件，明确 `toolbar`、`content` 和可选 `selectionBar` 三个插槽；环境、代理和内核页面均通过该结构组装领域组件。
- 环境页面结构固定为 `EnvironmentToolbar → EnvironmentTable → EnvironmentActionBar`，代理页面结构固定为 `ProxyToolbar → ProxyTable`，内核页面结构固定为 `KernelToolbar → KernelList`；Dialog 和 AlertDialog 作为页面级 overlay children 挂载。
- 本轮只调整 Renderer 组件关系，不改变数据、IPC、权限或用户流程范围。

## 2026-09-22 Electron sandbox Preload 运行时修复

- 真实 Electron 窗口验证发现 Electron 44 的 sandboxed Preload 不能直接执行 ESM `.mjs`，会导致 Renderer 空白并提示 `Cannot use import statement outside a module`。
- `electron.vite.config.ts` 现显式将 Preload 输出为 CommonJS `preload.cjs`，Main 窗口同步加载该文件；开发窗口重新验证后，Sidebar、面包屑、WorkspacePage、工具栏和 Empty 状态均正常渲染。
- 该修复不改变 IPC 契约、权限边界或 v0.1 功能范围。

## 2026-09-22 指定 B 端页面组件源码复核

- 固定读取 new-api `usage-logs/common` 和 shadcn-admin `tasks`、`users` 的页面入口、Provider、表格、过滤工具栏、批量操作和 Dialog/Sheet 源码。
- 研究结论已追加到 [B 端 CRUD、指纹环境管理与设置页研究](../research/ui-crud-settings-patterns.md) r7：页面采用 `Provider → Header/Toolbar → DataSurface → Pagination/BulkActions → Overlay` 组合；URL 列表状态与临时 overlay 状态分离；表格容器统一处理桌面、移动、加载、空态和分页。
- 本轮只更新源码研究记录，不修改 ContextWeave 页面功能或数据契约。

## 2026-09-22 指定页面技术栈复核

- 对照两个固定 commit 的 `package.json` 和页面 imports，确认 new-api 页面实际使用 React 19、Rsbuild、TanStack Router/Query/Table、Base UI、Tailwind 4、React Hook Form/Zod、i18next、React Query DataTable 封装和移动卡片布局。
- 确认 shadcn-admin tasks/users 实际使用 React 19、Vite、TanStack Router/Table、Radix UI、Tailwind 4、React Hook Form/Zod、React Context Provider、Vitest Browser/Playwright 和本地 fixture 数据。
- 研究文档已更新至 r8；本轮不引入这些项目的额外依赖，也不改变 ContextWeave v0.1 功能范围。

## 2026-09-22 TanStack Router 与 Base UI DataTable 实现

- 将 Renderer 路由从 `react-router` Data Mode 替换为 TanStack React Router File-based Routing：`src/routes/`、`routeTree.gen.ts`、`createHashHistory` 和 `RouterProvider` 已接入。
- 新增基于官方 Base UI Data Table 结构的通用 DataTable：Toolbar（左侧搜索/筛选、右侧操作/列显示）→ Table → Pagination；底层使用 TanStack Table v9 显式 feature 注册。
- 环境和代理列表已切换到通用 DataTable，原有领域列、行操作、状态筛选、空态和 Dialog overlay 保留。
- 删除未使用的 Sonner 组件和依赖；保留官网 Base UI Toast 组件作为通知实现基线；清理未被当前 Renderer 使用的全量 shadcn 组件文件和对应辅助依赖。

## 变更记录

- r9：按用户继续开发要求记录个人版菜单、CRUD 和 shadcn Select 决定，并纠正 r8 将整个 r6 范围描述为已完成的过度表述。
- r10：完成个人版菜单、环境/代理 CRUD 和 Select 迁移，补充 CRUD 边界测试及本机开发窗口验收；等待用户最终验收。
- r11：完成 GitHub B 端 CRUD、指纹环境管理和设置页源码研究，记录借鉴模式、许可证边界及 v0.1/后续版本范围。
- r12：按用户确认移除各业务页面顶部的大标题和说明，保留官方 Sidebar、顶部面包屑和卡片内部必要说明。
- r13：按研究结论补充环境/代理搜索、状态筛选和 DropdownMenu 行操作，并记录验证结果。
- r14：按 Base UI 组件规范统一表单 Field 组合、菜单分组、图标标记和间距写法，补充组件用法验证证据。
- r15：按 GitHub B 端与指纹浏览器项目研究重构 v0.1 主体内容骨架，统一工具栏、列表表面、资源行和轻量设置面板，减少边框与重复标题描述。
- r16：按用户反馈进一步移除卡片背景、圆角和阴影，统一为单一内容流与必要分隔线，并更新进度和验证待办。
- r17：按用户要求接入 `react-router` Data Mode，按菜单拆分路由页面和应用壳层，将环境/代理的工具栏、表格、动作条、表单和确认对话框拆到独立 feature components，并补充企业级 Renderer 目录与单职责组件规范。
- r18：按真实 Electron 窗口验证结果修正 sandboxed Preload 的 CommonJS 输出和加载路径，并记录主体组件布局验证证据。
- r19：复核 new-api `usage-logs/common`、shadcn-admin `tasks` 和 `users` 的具体组件树与状态分层，并追加到研究文档。
- r20：补充两个项目的页面技术栈、数据请求、表格、UI primitives、表单和测试工具对照。
